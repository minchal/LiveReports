<!DOCTYPE html>
<html lang="pl">
<head>
  <title>Livereports</title>
  <meta name="description" content="" />
  <meta name="keywords" content="" />
  <%= stylesheet_link_tag "style" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  </head>
<body>
<div id="site">
	<div id="top">
		<h1><a href="/"><span>Logo serwisu</span></a></h1>
		
		<ul id="menu">
			<li><%= link_to "Trwające relacje", reports_path %></li>
			<li><%= link_to "Archiwum", archives_path %></li>
			
			<% if current_user %>
				<% if User.find(session[:user_id]).email == "michal@o2.pl" %>
				<li><a href="/admin">Panel administratora</a></li>
				<% end %>
			<li><%= link_to "Wyloguj", log_out_path %></li>
			<% else %>
			<li><%= link_to "Zarejestruj się", sign_up_path %></li>
			<li><%= link_to "Zaloguj się", log_in_path %></li>
			<% end %>
		</ul>
	</div>
	<div id="body">
		<% flash.each do |name, msg| %>
		<%= content_tag :div, msg, :id => "flash_#{name}" %>
		<% end %>
		
		<!--<% if current_user %>
		Witaj <%= current_user.email %>!
		<% end %>-->
		
		<%= yield %>
	</div>
	<div id="foot">
		<p>© NazwaStrony.pl</p>
	</div>
</div>
</body>
</html>
