<%= form_for(@chat_entry) do |f| %>
  <% if @chat_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chat_entry.errors.count, "error") %> prohibited this chat_entry from being saved:</h2>

      <ul>
      <% @chat_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
